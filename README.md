Creating a virtual environment ``python3 -m venv venv``

Activating Virtual environment ``source venv/bin/activate``

Selecting Python interpreter ``Cmd + Shift + P``

Installing packages ``pip install pandas sqlalchemy psycopg2-binary python-dotenv``

Adding requirements to requirement.txt ``pip freeze>requirement.txt``
